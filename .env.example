# .env.example
# Copy to .env and fill in. NEVER commit .env.

# Minds Builder API key (JWT) — from build.hellominds.ai profile
MINDS_BUILDER_API_KEY=

# Discord bot token — treated as a private key
BOT_TOKEN=
